/**
 * The sidebar, as data.
 *
 * Empty for now — the only page in `src/content/docs/` is the temporary
 * `index.mdx` placeholder. A Starlight sidebar entry that names a `slug`
 * fails the build if that slug isn't in the docs collection, so this stays
 * empty until real pages (and their groups) land.
 */
export const docsSidebarGroups = [];

export const changelogEntry = { label: 'Changelog', slug: 'changelog' };

